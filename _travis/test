#!/bin/bash
set -ex
cd build

# Call CTest test runner
ctest --output-on-failure

# If on Linux with GCC: print coverage report
if [ "$TRAVIS_OS_NAME" = "linux" -a "$CXX" = "g++" ] ; then
	ctest -T coverage
fi
